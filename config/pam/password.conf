# PAM configuration for NothingLess lockscreen
# Minimal config: just password auth, no faillock/systemd-homed
auth        sufficient    pam_unix.so     likeauth nullok
auth        required      pam_deny.so
account     required      pam_unix.so
