# Cava configuration for NothingLess — Brainx preset
# Placed at ~/.config/nothingless/cava.config
#
# 32 bars, raw output mode for QML parsing.

[general]
framerate = 30
bars = 32
autosens = 1
sensitivity = 100

[output]
method = raw
raw_target = /dev/stdout
data_format = ascii
ascii_max_range = 7
bit_format = 16bit

[smoothing]
noise_reduction = 50
monstercat = 1
waves = 0

[input]
method = pulse
source = auto
